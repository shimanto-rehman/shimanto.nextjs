# Environment Variables Example
# Copy this file to .env.local and fill in your actual values

# Google Scholar API (via SerpAPI)
# Required for /api/scholar endpoint
# Get your API key from: https://serpapi.com/
SCHOLAR_AUTHOR_ID=your_google_scholar_author_id_here
SERPAPI_KEY=your_serpapi_key_here

# GitHub API Token (Optional)
# Used for repositories page to fetch GitHub data
# If not provided, repositories page will use public API (rate-limited)
# Get your token from: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_personal_access_token_here

# Next.js Public Environment Variables
# These are exposed to the browser and should start with NEXT_PUBLIC_
# Used for generating absolute URLs for API routes
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Note: In production, these should be set in your hosting platform's environment variables
# (e.g., Vercel, Netlify, etc.)
